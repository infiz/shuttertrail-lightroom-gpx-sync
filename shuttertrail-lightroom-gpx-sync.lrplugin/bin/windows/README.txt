Place the official 64-bit ExifTool Windows executable here as:

  exiftool.exe

Keep the official exiftool_files directory beside it. The expected layout is:

  bin/windows/exiftool.exe
  bin/windows/exiftool_files/...

Download ExifTool from https://exiftool.org/
